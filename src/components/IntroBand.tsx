const IntroBand = () => (
  <div className="animate-rise w-full bg-band px-6 py-8 text-bandFg transition-colors duration-500 sm:px-10">
    <p className="mx-auto max-w-2xl font-body text-sm italic leading-relaxed sm:text-base">
      Learn by building — the rest follows.
    </p>
  </div>
);

export default IntroBand;
